# `04` PATCH Update Title

Send a `PATCH` request to `/items/2` with this body:

```json
{
  "title": "Patched title"
}
```

Inspect the response and identify the value that proves the title update worked.

Expected result: `"Patched title"`
