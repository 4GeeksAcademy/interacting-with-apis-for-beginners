# `05` PATCH Update Status

Envia una peticion `PATCH` a `/items/1` con este body:

```json
{
  "done": true
}
```

Inspecciona la respuesta e identifica el valor que demuestra que el estado del recurso cambio correctamente.

Resultado esperado: `true`
