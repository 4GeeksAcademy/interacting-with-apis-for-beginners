# `01` POST Create Item

Envia una peticion `POST` a `/items` con este body:

```json
{
  "title": "Practice POST requests"
}
```

Inspecciona la respuesta despues de crear el recurso e identifica el valor que coincide con el resultado esperado.

Resultado esperado: `3`
