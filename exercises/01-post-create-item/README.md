# `01` POST Create Item

Send a `POST` request to `/items` with this body:

```json
{
  "title": "Practice POST requests"
}
```

Inspect the response after creating the resource and identify the value that matches the expected result.

Expected result: `3`
