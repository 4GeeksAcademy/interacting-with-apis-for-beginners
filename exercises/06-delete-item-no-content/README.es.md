# `06` DELETE No Content

Envia una peticion `DELETE` a `/items/1`.

No intentes parsear JSON del body de la respuesta.

Inspecciona el objeto `response` e identifica el valor que confirma que la peticion fue exitosa con el status RESTful correcto.

Resultado esperado: `204`
